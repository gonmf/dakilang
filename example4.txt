version

author(X, Y) :- author(X, Y).

author('roberto', X)?


else(X, Y) :- other(Y, X).
other(X, Y) :- other(X, Y).

listing

other('roberto', X)?

select_table 1

parent("john", "mary").
parent("john", "maria").
parent("victor", "john").
grandparent(X, Y) :- parent(X, Z) & parent(Z, Y).
grandparent(A, B)?
