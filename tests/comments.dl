# Some tests with comments
          # Bellow

mammal(A) :- # Not a perfect definition \
  blood(A, 'warm'), \
  (hasHair(A); hashFur(A)), \
  # laysEggs(A), Not all of them lay legs! \
  producesMilk(A).

listing
