is_power(N, NN) :- eql(0 + (N * N) + ((1 - 3) + 2), NN, Res).

listing

is_power(3, 9)? % Yes

is_power(4, 12)? % No

print_more(N) :- print(N, Out), print(N * 2, Out), print(N * N, Out), print((N + 1) * N + 1, Out).
print_more(5)?
