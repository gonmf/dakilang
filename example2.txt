actor(chaplin).
athlete(ronaldo).
athlete(messi).
politician(ghandi).
politician(arnold).
actor(arnold).
athlete(arnold).

famous(X) :- actor(X) | \
             athlete(X) | \
             politician(X).

listing

famous(X)?

other(X) :- abc(X, gual).
other(X) :- abc(X, guali).
other(X) :- abc(X, gual). % This instruction will produce a warning

other(X) :- abc(X, guali) | abc(X, gual).

listing
